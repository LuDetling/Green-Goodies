{% extends 'base.html.twig' %}

{% block title %}
	{{product.name}}
{% endblock %}

{% block body %}
	<main class="content-product">

		<img src="{{asset(product.picture)}}" alt="{{product.name}}">
		<div class="right-product">
			<div class="right-product-content">
				<h1>{{ product.name }}</h1>
				<div class="price-product">{{ product.price|number_format(2, ',') }}€</div>
				<div class="label-description">Description</div>
				<p class="description">{{ product.fullDescription }}</p>
				<button class="add btn-darkgrey">Ajouter au pannier</button>
			</div>
		</div>
	</main>
{% endblock %}
